services:
  db:
    image: postgres:15-alpine
    container_name: flowbotomat_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5451:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init:/docker-entrypoint-initdb.d
    networks:
      - flowbotomat_network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flowbotomat_backend
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV}
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      APP_URL: ${APP_URL}
      WAHA_BASE_URL: ${WAHA_BASE_URL}
      WAHA_API_KEY: ${WAHA_API_KEY}
      SUMIT_CompanyID: ${SUMIT_CompanyID}
      SUMIT_APIKey: ${SUMIT_APIKey}
      SUMIT_APIPublicKey: ${SUMIT_APIPublicKey}
      API_URL: ${VITE_API_URL}
    ports:
      - "3749:4000"
    volumes:
      - ./backend:/app
      - /app/node_modules
      - uploads_data:/app/uploads
    depends_on:
      - db
    networks:
      - flowbotomat_network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: flowbotomat_frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: ${VITE_API_URL}
      VITE_SUMIT_PUBLIC_KEY: ${SUMIT_APIPublicKey}
    ports:
      - "3748:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - flowbotomat_network

  backup:
    image: postgres:15-alpine
    container_name: flowbotomat_backup
    restart: unless-stopped
    environment:
      POSTGRES_HOST: db
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - ./scripts/backup.sh:/backup.sh:ro
      - backups_data:/backups
    depends_on:
      - db
    networks:
      - flowbotomat_network
    entrypoint: /bin/sh
    command: -c "chmod +x /backup.sh && echo '0 3 * * * /backup.sh >> /var/log/backup.log 2>&1' | crontab - && crond -f -l 2"

networks:
  flowbotomat_network:
    driver: bridge

volumes:
  postgres_data:
  uploads_data:
  backups_data: